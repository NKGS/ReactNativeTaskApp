import React from 'react';
import { View, Text } from 'react-native';

const Screen2 = () => {
    return (
        <View style={{flex:1}}>
            <Text style={{fontSize:40, flex:1, textAlign:'center', color:'red'}}>Screen2</Text>
        </View>
    )
}

export default Screen2